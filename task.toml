schema_version = "1.1"

[task]
name = "talents/a161888-bbolt-integrity-sentinel"
description = "Repair corruption traversal, transaction panic cleanup, close coordination, rollback, durable publication, cursor state, snapshot copying, file growth, meta recovery, and bucket-move isolation defects in bbolt."
keywords = ["go", "bbolt", "database", "integrity", "transactions", "concurrency", "recovery", "cursor", "snapshot", "lifecycle", "isolation"]

[metadata]
difficulty = "expert"
category = "programming"
tags = ["l4", "storage-engine", "corruption", "fault-injection", "concurrency", "judge-grading"]

[verifier]
timeout_sec = 600.0

[agent]
timeout_sec = 1800.0

[environment]
build_timeout_sec = 3600.0
cpus = 4
memory_mb = 8192
storage_mb = 10240
gpus = 0
allow_internet = false
mcp_servers = []

[verifier.env]

[solution.env]
